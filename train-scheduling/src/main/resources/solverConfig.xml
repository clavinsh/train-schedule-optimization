<?xml version="1.0" encoding="UTF-8"?>
<solver xmlns="https://timefold.ai/xsd/solver" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="https://timefold.ai/xsd/solver https://timefold.ai/xsd/solver/solver.xsd">
    <solutionClass>com.example.domain.TrainSchedule</solutionClass>
    <entityClass>com.example.domain.ScheduledTrip</entityClass>
    <scoreDirectorFactory>
        <constraintProviderClass>com.example.solver.TrainScheduleConstraintProvider</constraintProviderClass>
    </scoreDirectorFactory>

    <!-- Termination and multithreading configured via application.properties -->

    <!-- Construction Heuristic: ALLOCATE_ENTITY_FROM_QUEUE is faster for multi-variable entities -->
    <constructionHeuristic>
        <constructionHeuristicType>ALLOCATE_ENTITY_FROM_QUEUE</constructionHeuristicType>
    </constructionHeuristic>

    <!-- Local Search: Late Acceptance - accepts if better than score N steps ago -->
    <localSearch>
        <unionMoveSelector>
            <changeMoveSelector/>
            <swapMoveSelector/>
        </unionMoveSelector>

        <acceptor>
            <lateAcceptanceSize>200</lateAcceptanceSize>
        </acceptor>

        <forager>
            <acceptedCountLimit>4</acceptedCountLimit>
        </forager>
    </localSearch>
</solver>